<%= form_with(model: listing) do |form| %>
  <% if listing.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(listing.errors.count, "error") %> prohibited this listing from being saved:</h2>

      <ul>
        <% listing.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :artist, style: "display: block" %>
    <%= form.text_field :artist %>
  </div>

  <div>
    <%= form.label :label, style: "display: block" %>
    <%= form.text_field :label %>
  </div>

  <%# <div>
      <% @test_array = @conditions.map do |option| %>
      <%# <% format_condition(option) %> 
      <%# <% end %>  
      <%# <%= form.label :condition, style: "display: block" %> 
       <%# <%= form.select :condition, @test_array %>
      <%# <%= form.select :condition, @conditions.map {|c| [c.name, c.id]} %> 
  <%# </div> %> 

    <div>
    <% @conditions.each do |option| %>
      <%= form.label :condition, format_condition(option) %>
      <%= form.radio_button :condition, option %>
    <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.rich_text_area :description %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.number_field :price %>
  </div>

  <div>
    <%= form.label :genre_id, style: "display: block" %>
    <%= form.select :genre_id, @genres.map {|g| [g.name, g.id]} %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
